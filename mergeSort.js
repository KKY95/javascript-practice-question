function meregeSort(arr)
{
    if(arr.length<1)
    return arr;
}
for(let i=0;i<arr.length;i++)
{
const mid=Math.floor(arr.length/2);
const leftHalf=arr.slice(0,mid);
const rightHalf=arr.slice(mid+1);
}
const array=[2,9,3,1,5,6];
mergeSort(array);
console.log(array);